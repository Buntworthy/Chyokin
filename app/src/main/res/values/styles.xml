<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimarySave</item>
        <item name="colorPrimaryDark">@color/colorPrimarySaveDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimarySaveDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NumberButton" parent="android:Widget.Holo.Button.Borderless">
        <item name="android:layout_width">64dp</item>
        <item name="android:layout_height">64dp</item>
        <item name="android:background">@drawable/round_button_number</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textColor">@color/colorPrimarySave</item>
        <item name="android:textSize">24sp</item>
        <item name="android:fontFamily">sans-serif-light</item>

    </style>

    <style name="TotalButtonText" parent="android:Widget.Holo.Button.Borderless">
        <item name="android:typeface">normal</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="TotalButtonText.SaveButton" >
        <item name="android:background">@drawable/round_button_save</item>
        <item name="android:padding">0dp</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:layout_weight">0</item>
        <item name="android:textSize">32sp</item>
    </style>

    <style name="TotalButtonText.WasteButton">
        <item name="android:background">@drawable/round_button_waste</item>
        <item name="android:layout_weight">0</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
